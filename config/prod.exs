import Config

# Production configuration
config :logger, level: :info

# In production, you should set these via environment variables
# AWS credentials will be pulled from IAM role or environment variables
